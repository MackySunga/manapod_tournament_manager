# manapod_tournament_manager
Mana Pod is a lightweight PHP tournament manager for MTG Commander pod events. Create games/rounds, generate pod pairings, run timers, record/lock scores, and publish standings/reports. Includes player portal + live display. JSON storage, no DB/Composer, Bootstrap 5.
